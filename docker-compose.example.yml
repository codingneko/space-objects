version: "3"

services:
  space-track:
    build: .
    restart: always
    ports:
      - "80:8080"
    volumes:
      - db:~/space-objects/db
    environment:
      - ST_USERNAME=
      - ST_PASSWORD=

volumes:
  db:
    external: true
